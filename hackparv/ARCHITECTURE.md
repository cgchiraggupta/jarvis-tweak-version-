# Architecture Overview

Visual representation of the integration architecture.

## 🎯 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          USER INPUT                              │
│                                                                   │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                  │
│  │   CLI    │    │  Voice   │    │   GUI    │                  │
│  │  Input   │    │  Input   │    │  Prompt  │                  │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘                  │
│       └───────────────┴───────────────┘                         │
│                       │                                          │
│              "open Safari and go to github.com"                  │
└───────────────────────┼──────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│               SELF-OPERATING COMPUTER (Python)                   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  main.py                                                 │   │
│  │  • Parse command-line arguments                          │   │
│  │  • Initialize configuration                              │   │
│  │  • Start main loop                                       │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │  operate.py                                              │   │
│  │  • Orchestrate workflow                                  │   │
│  │  • Manage message history                                │   │
│  │  • Execute operations                                    │   │
│  │  • Control iteration loop                                │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │  apis.py                                                 │   │
│  │  • Route to correct model                                │   │
│  │  • Handle model-specific logic                           │   │
│  │  • get_next_action(model, messages, objective)          │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│         ┌─────────────┴─────────────┐                           │
│         │  Model: "assistant"?      │                           │
│         └─────────────┬─────────────┘                           │
│                       │ Yes                                      │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │  assistant_adapter.py ★ NEW                              │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │  1. capture_screen_with_cursor()                 │   │   │
│  │  │     └─> screenshots/screenshot.png               │   │   │
│  │  │                                                   │   │   │
│  │  │  2. encode_screenshot()                          │   │   │
│  │  │     └─> base64_encoded_image                     │   │   │
│  │  │                                                   │   │   │
│  │  │  3. prepare_prompt(objective)                    │   │   │
│  │  │     └─> "Help me: open Safari..."                │   │   │
│  │  │                                                   │   │   │
│  │  │  4. call_assistant_api()                         │   │   │
│  │  │     POST http://localhost:4001/analyze           │   │   │
│  │  │     └─> {image, prompt, objective}               │   │   │
│  │  │                                                   │   │   │
│  │  │  5. parse_response()                             │   │   │
│  │  │     └─> [operations]                             │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────┼──────────────────────────────────────────┘
                        │ HTTP POST
                        │
┌───────────────────────▼──────────────────────────────────────────┐
│               ASSISTANT API (Node.js)                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  http_server.js ★ NEW                                   │    │
│  │  ┌──────────────────────────────────────────────────┐  │    │
│  │  │  POST /analyze endpoint                          │  │    │
│  │  │  ┌────────────────────────────────────────────┐  │  │    │
│  │  │  │  1. Receive request                        │  │  │    │
│  │  │  │     • image (base64)                       │  │  │    │
│  │  │  │     • prompt                                │  │  │    │
│  │  │  │     • objective                             │  │  │    │
│  │  │  │                                             │  │  │    │
│  │  │  │  2. Prepare messages                       │  │  │    │
│  │  │  │     • System prompt (computer control)     │  │  │    │
│  │  │  │     • User message with image              │  │  │    │
│  │  │  │                                             │  │  │    │
│  │  │  │  3. Call OpenAI API                        │  │  │    │
│  │  │  │     openai.chat.completions.create()      │  │  │    │
│  │  │  │     model: "gpt-4o"                        │  │  │    │
│  │  │  │                                             │  │  │    │
│  │  │  │  4. Parse response                         │  │  │    │
│  │  │  │     • Clean JSON                            │  │  │    │
│  │  │  │     • Validate operations                   │  │  │    │
│  │  │  │                                             │  │  │    │
│  │  │  │  5. Return operations                      │  │  │    │
│  │  │  │     {operations: [...], model, timestamp}  │  │  │    │
│  │  │  └────────────────────────────────────────────┘  │  │    │
│  │  └──────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                   │
│  Other endpoints:                                                │
│  • GET  /health   - Health check                                │
│  • POST /query    - Text-only queries                           │
│                                                                   │
└───────────────────────┼──────────────────────────────────────────┘
                        │ HTTPS
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    OPENAI API                                    │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  GPT-4 Vision (gpt-4o)                                  │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │  1. Receive image + prompt                       │  │   │
│  │  │  2. Analyze screen state visually                │  │   │
│  │  │  3. Reason about objective                       │  │   │
│  │  │  4. Determine next action                        │  │   │
│  │  │  5. Generate JSON operations                     │  │   │
│  │  │                                                   │  │   │
│  │  │  Example output:                                 │  │   │
│  │  │  [                                                │  │   │
│  │  │    {                                              │  │   │
│  │  │      "operation": "click",                       │  │   │
│  │  │      "x": 156,                                   │  │   │
│  │  │      "y": 1024,                                  │  │   │
│  │  │      "thought": "Clicking Safari icon in dock"  │  │   │
│  │  │    }                                              │  │   │
│  │  │  ]                                                │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────┼──────────────────────────────────────────┘
                        │ JSON Response
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│               SELF-OPERATING COMPUTER (Python)                   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  operate.py                                              │   │
│  │  • Receive operations from assistant_adapter             │   │
│  │  • operate(operations, model)                            │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│                       ▼                                          │
│  For each operation:                                            │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  operating_system.py                                     │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │  • click(x, y)    → PyAutoGUI.click()            │  │   │
│  │  │  • write(text)    → PyAutoGUI.typewrite()        │  │   │
│  │  │  • press(keys)    → PyAutoGUI.hotkey()           │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                       │                                          │
│                       ▼                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Check if done?                                          │   │
│  │  • Yes: Print summary and exit                           │   │
│  │  • No: Capture new screenshot and repeat                 │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                      macOS SYSTEM                                │
│                                                                   │
│  • Applications launch                                           │
│  • Mouse moves and clicks                                        │
│  • Keyboard input                                                │
│  • Screen updates                                                │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Request Flow

```
┌──────┐   1. Command    ┌────────────┐
│ User ├───────────────→ │   CLI      │
└──────┘                 │   Input    │
                         └──────┬─────┘
                                │
                         2. Capture
                                │
                         ┌──────▼─────┐
                         │ Screenshot │
                         │ (1920x1080)│
                         └──────┬─────┘
                                │
                         3. Encode
                                │
                         ┌──────▼─────┐
                         │ Base64     │
                         │ (~2-5 MB)  │
                         └──────┬─────┘
                                │
                         4. HTTP POST
                                │
                         ┌──────▼──────────┐
                         │ Assistant API   │
                         │ localhost:4001  │
                         └──────┬──────────┘
                                │
                         5. Forward to AI
                                │
                         ┌──────▼──────────┐
                         │ OpenAI GPT-4    │
                         │ Vision API      │
                         └──────┬──────────┘
                                │
                         6. AI Analysis
                                │
                         ┌──────▼──────────┐
                         │ JSON Operations │
                         │ [click, write]  │
                         └──────┬──────────┘
                                │
                         7. Parse & Execute
                                │
                         ┌──────▼──────────┐
                         │ PyAutoGUI       │
                         │ Actions         │
                         └──────┬──────────┘
                                │
                         8. Screen Update
                                │
                         ┌──────▼──────────┐
                         │ macOS Desktop   │
                         │ Changes         │
                         └─────────────────┘
```

### Timing Diagram

```
Time  │ Component                  │ Action
──────┼───────────────────────────┼─────────────────────────────
0s    │ User                       │ Types: operate --model=assistant --prompt="open Safari"
0.1s  │ self-operating-computer    │ Parses args, initializes config
0.5s  │ self-operating-computer    │ Captures screenshot (screenshot.png)
1.0s  │ assistant_adapter          │ Encodes screenshot to base64
1.2s  │ assistant_adapter          │ Prepares prompt with objective
1.5s  │ assistant_adapter          │ POST to http://localhost:4001/analyze
1.6s  │ Assistant HTTP API         │ Receives request
1.7s  │ Assistant HTTP API         │ Forwards to OpenAI GPT-4 Vision
2.0s  │ OpenAI API                 │ Processing (AI inference)...
      │                            │
5.0s  │ OpenAI API                 │ Returns: [{"operation":"click","x":156,"y":1024}]
5.1s  │ Assistant HTTP API         │ Parses and validates response
5.2s  │ Assistant HTTP API         │ Returns JSON to adapter
5.3s  │ assistant_adapter          │ Parses operations
5.4s  │ operate.py                 │ Executes click at (156, 1024)
6.4s  │ PyAutoGUI                  │ Moves mouse and clicks
7.4s  │ macOS                      │ Safari launches
8.4s  │ self-operating-computer    │ Captures new screenshot
9.4s  │ assistant_adapter          │ Checks if objective complete
10.0s │ operate.py                 │ Done! Prints summary
```

## 🧩 Component Interactions

### Component Diagram

```
┌─────────────────────────────────────────────────────────┐
│                   Configuration Layer                    │
├──────────────┬──────────────────────────────────────────┤
│  config.py   │  Environment variables, API keys         │
│  .env        │  OpenAI key, ports, settings             │
└──────────────┴──────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│                    Application Layer                     │
├──────────────┬──────────────────────────────────────────┤
│  main.py     │  Entry point, arg parsing                │
│  operate.py  │  Main orchestration, message history     │
└──────────────┴──────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│                     Model Layer                          │
├──────────────┬──────────────────────────────────────────┤
│  apis.py     │  Model routing, dispatcher               │
│              │  • gpt-4-with-ocr                         │
│              │  • claude-3                               │
│              │  • assistant ★                            │
│              │  • llava                                  │
│              │  • qwen-vl                                │
└──────────────┴──────────────────────────────────────────┘
                              │
                  ┌───────────┴────────────┐
                  │                        │
                  ▼                        ▼
┌────────────────────────────┐  ┌─────────────────────────┐
│  assistant_adapter.py ★    │  │  Other Model Adapters   │
│  ┌──────────────────────┐  │  │  • call_gpt_4o()       │
│  │ AssistantAdapter     │  │  │  • call_claude_3()     │
│  │  • encode_screenshot │  │  │  • call_ollama()       │
│  │  • prepare_prompt    │  │  └─────────────────────────┘
│  │  • call_api          │  │
│  │  • parse_response    │  │
│  └──────────────────────┘  │
└────────────┬───────────────┘
             │ HTTP
             ▼
┌─────────────────────────────────────────────────────────┐
│              Network / API Layer                         │
├──────────────────────────────────────────────────────────┤
│  HTTP POST http://localhost:4001/analyze               │
│  Request: {image, prompt, objective}                    │
│  Response: {operations: [...], model, timestamp}        │
└─────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│           Assistant HTTP Server (Node.js)                │
├──────────────┬──────────────────────────────────────────┤
│  http_server │  Express.js server                       │
│  ★ NEW       │  • POST /analyze                         │
│              │  • GET /health                            │
│              │  • POST /query                            │
└──────────────┴──────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│                 OpenAI Integration                       │
├──────────────────────────────────────────────────────────┤
│  openai.chat.completions.create()                       │
│  model: "gpt-4o"                                        │
│  messages: [{role, content: [text, image]}]            │
└─────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│              Operations / Execution Layer                │
├──────────────┬──────────────────────────────────────────┤
│  operate.py  │  parse operations, execute each          │
│  operating_  │  • click → PyAutoGUI.click()            │
│  system.py   │  • write → PyAutoGUI.typewrite()        │
│              │  • press → PyAutoGUI.hotkey()            │
│              │  • done  → exit                           │
└──────────────┴──────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│                  System / OS Layer                       │
├──────────────────────────────────────────────────────────┤
│  macOS APIs                                              │
│  • Screen Recording (capture screenshots)               │
│  • Accessibility (control mouse/keyboard)               │
│  • Application launching                                 │
└─────────────────────────────────────────────────────────┘
```

## 📦 Module Dependencies

### Python Dependencies

```
operate/
├── main.py
│   └── imports: operate.py, config.py
│
├── operate.py
│   └── imports: apis.py, prompts.py, config.py,
│                operating_system.py, style.py
│
├── config.py
│   └── imports: openai, anthropic, google.generativeai,
│                ollama, dotenv
│
├── models/
│   ├── apis.py
│   │   └── imports: assistant_adapter.py, prompts.py,
│   │               screenshot.py, ocr.py, label.py
│   │
│   └── assistant_adapter.py ★ NEW
│       └── imports: requests, PIL, base64, json,
│                   config.py, prompts.py, screenshot.py
│
└── utils/
    ├── screenshot.py
    ├── ocr.py
    ├── label.py
    ├── operating_system.py
    └── style.py
```

### Node.js Dependencies

```
jarvis/server/
├── index.js (WebSocket server)
│   └── requires: ws, http, dotenv, audiofunctions.js, actions.js
│
├── http_server.js ★ NEW
│   └── requires: express, cors, openai, dotenv
│
└── actions.js
    └── requires: child_process (exec)
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────┐
│               Security Boundaries                    │
└─────────────────────────────────────────────────────┘

User's Mac (localhost)
┌───────────────────────────────────────────────────┐
│                                                    │
│  ┌──────────────────┐    ┌──────────────────┐   │
│  │  Python          │    │  Node.js         │   │
│  │  self-operating  │←──→│  Assistant API   │   │
│  │  Port: N/A       │HTTP│  Port: 4000,4001 │   │
│  └──────────────────┘    └────────┬─────────┘   │
│         │                          │              │
│         │ No network              │ HTTPS        │
│         │                          │              │
│  ┌──────▼──────────┐              │              │
│  │  macOS APIs     │              │              │
│  │  • Screenshots  │              │              │
│  │  • Accessibility│              │              │
│  └─────────────────┘              │              │
│                                    │              │
└────────────────────────────────────┼──────────────┘
                                     │
                    Internet         │ TLS 1.3
                   Boundary          │
                                     │
┌────────────────────────────────────▼──────────────┐
│  ┌──────────────────────────────────────────┐    │
│  │  OpenAI API                              │    │
│  │  • GPT-4 Vision                          │    │
│  │  • Secure HTTPS                          │    │
│  │  • API Key Authentication                │    │
│  └──────────────────────────────────────────┘    │
└───────────────────────────────────────────────────┘

Security Measures:
✅ API keys in .env (gitignored)
✅ All local communication over localhost
✅ No data persistence (except local screenshots)
✅ Minimal OS permissions (Screen Recording + Accessibility)
✅ HTTPS for OpenAI API
✅ No external network exposure
```

---

## 📊 Performance Characteristics

### Typical Operation Timeline

| Phase | Duration | Component | Bottleneck |
|-------|----------|-----------|------------|
| Screenshot capture | ~100-500ms | Python/mss | Disk I/O |
| Base64 encoding | ~50-100ms | Python | CPU |
| HTTP request | ~5-10ms | Network | localhost (minimal) |
| OpenAI API | ~3-8s | OpenAI | API processing |
| JSON parsing | ~10-50ms | Python | Minimal |
| Action execution | ~500-1000ms | PyAutoGUI | UI responsiveness |
| **Total** | **~4-10s** | | **OpenAI API** |

### Resource Usage

- **Memory**: ~200-500MB (Python + Node.js + Screenshots)
- **CPU**: Peaks during screenshot capture and API calls
- **Network**: Only outbound to OpenAI API (~2-5MB per request)
- **Disk**: Screenshots stored locally (~1-3MB each)

---

**This architecture enables seamless AI-powered computer control while maintaining security, performance, and extensibility.**




